<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ani.ccyl.leg.persistence.mapper.DailyTotalScoreMapper">
    <select id="findTop20" parameterType="java.lang.String" resultType="com.ani.ccyl.leg.persistence.po.ShareRelationPO" >
        select * from T_DAILY_TOTAL WHERE log_date =#{date} ORDER BY SCORE DESC limit 20;
    </select>

    <select id="findRankByAccountId" parameterType="java.util.Map" resultType="java.lang.Integer">
       select ranks from (SELECT *, @rownum := @rownum +1 AS ranks FROM T_DAILY_TOTAL,(SELECT@rownum :=0) r WHERE LOG_DATE=${logDate} ORDER BY SCORE DESC) t WHERE t.accountId=#{accountId}
    </select>

</mapper>