<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ani.ccyl.leg.persistence.mapper.AwardMapper">
    <select id="findIsAward" parameterType="java.lang.Integer" resultType="java.lang.Boolean">
        SELECT CASE t.awardCounts
            WHEN 0 THEN FALSE
            ELSE TRUE
            END
        FROM (SELECT count(*) as awardCounts FROM t_award WHERE account_id=#{accountId} AND date_format(create_time,'%Y-%m-%d')=date_format(now(),'%Y-%m-%d')) t
    </select>
</mapper>