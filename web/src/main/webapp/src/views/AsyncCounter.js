import React, {Component} from 'react';

class AsyncCounter extends Component {
    render() {
        return (
            <div>AsyncCounter component</div>
        )
    }
}

export default AsyncCounter;